// Add an element to the end of an array.

// Algorithm
// 1. Take an array and hold it in variable
// 2. Use push method to add the element in the end of the array
// 3. Print the result

// Pseudo-code
// array = [arr1,arr2,arr3];
// newArray = array.push(arr4)
// print array

// Source code
const myArr = [1, 3, 4];
const newArr = myArr.push(99);
console.log(myArr);
